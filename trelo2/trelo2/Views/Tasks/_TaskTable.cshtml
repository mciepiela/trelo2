@using trelo2.Models
@model IEnumerable<trelo2.Models.Task>


<div class="progress"><div class="progress-bar progress-bar-warning progress-bar-striped" role="progressbar" style="width: @(Html.AttributeEncode(ViewBag.Percent))%"></div></div>


<table class="table">

    
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.CheckBoxFor(modelItem => item.IsReady, new { id = item.Id, @class = "ActiveCheck" })
            </td>
            <td>
                @if (!item.IsReady)
                {
                    @Html.DisplayFor(modelItem => item.Body)

                }
                else
                {
                    <div class="finishedTask">@Html.DisplayFor(modelItem => item.Body)</div>
                }
            </td>

            <td>
                @Html.ActionLink("Edytuj", "Edit", new {id = item.Id}) |

                @Html.ActionLink("Usuń", "Delete", new {id = item.Id})
            </td>
        </tr>
    }

</table>

<script src="~/Custom_Scripts/AddCheckboxListner.js"></script>